# scoop-bucket

Scoop manifests for my tools.

```powershell
scoop bucket add mopi1402 https://github.com/mopi1402/scoop-bucket
scoop install terminal-scheme-info
```

The manifests are regenerated by each tool's release workflow from the
checksums of its GitHub release.
